apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: microservice-engine-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
  - host: microservice-engine.example.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: microservice-engine-service
            port:
              number: 80
